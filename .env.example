PORT=8080

# Shared secret the Amsterdam backend must send in the x-relay-token header.
# This is a public-market-data relay only (no funds, no user data), but a
# long random value still keeps it from being scraped by strangers.
# Generate one with: openssl rand -hex 32
RELAY_AUTH_TOKEN=replace-with-a-long-random-secret
